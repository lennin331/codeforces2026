#include<bits/stdc++.h>
#define MOD 998244353;
//A ribbon for tomorrow
//has n beads of 0 or 1 (white and black)  
//select l,r where s[l] == s[r] and reverse the string inbetween
//
//approach - using two pointers and combinations
int main(){
  int tt{}; std::cin>>tt;
  while(tt--){
    int n{};std::cout<<n;
    std::string beads;std::cin>>beads;
    long long res = 0;
    int r = 0, l = n;
    while(r!=l){
      if(!beads[l]^beads[r]){
        res = 
      }
    }

  }
}
